## this shoud be run of the tree file that does not have dots.
## it will strip old tree labels matching the format 'XXX':
## it will also remove zeroed branch lengths
sed -E 's/:0\.0+([),])/\1/g' $1 | sed -E "s/'[0-9]+'://g" > final_tree_2.nwk
